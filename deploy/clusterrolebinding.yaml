apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: resume-portfolio-binding
subjects:
  - kind: ServiceAccount
    name: resume-portfolio
    namespace: resume-portfolio
roleRef:
  kind: ClusterRole
  name: resume-portfolio
  apiGroup: rbac.authorization.k8s.io